{
  "name": "factory-publish-lab",
  "version": "0.0.0",
  "private": true,
  "description": "Laboratorio descartavel para o primeiro teste de PUBLICACAO da Forja Factory (branch remota + PR draft). Sem dependencias, sem servicos externos, sem segredos.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "test": "node --test \"tests/*.test.ts\""
  }
}
